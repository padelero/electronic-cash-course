
export const API_CONFIG = {
  // En un entorno de producción, esta URL debería ser la dirección de tu servidor backend
  // Por ahora, usamos una URL de ejemplo que deberás actualizar cuando tengas el backend listo
  BASE_URL: 'http://localhost:3000/api',
  ENDPOINTS: {
    COURSES: '/courses',
    USERS: '/users',
    AUTH: '/auth',
    REWARDS: '/rewards'
  }
};
